\chapter{Implementation}

% Number layout FUCKERY needed. Could really help!!

This chapter describes the implementation of the different components of the analysis tool, working from the naive 
baseline, exploring matrix representations and computations, continuing to data-driven optimisations, and concluding 
with acceleration of compute using additional hardware.

\section{Naive Baseline}

As described in Section \ref{intro:methodology}, classical techniques for 
ILP limit studies involve a sequential algorithm iterating through the program trace. We take inspiration from PerPI's 
use of the Intel Pin Tool \cite{perpi}. The algorithm presented successfully finds the longest path in the DDG described
in Section \ref{prep:register-only-chains}.

For each instruction in the program trace, the algorithm:
\begin{enumerate}
  \item  Examines static read dependencies, and computes the size of the largest chain of dependencies ending in a dependent register.
  \item  Examines static write dependencies, and propagates the longest chain of dependencies to the next iteration.
\end{enumerate}

\begin{lstlisting}[language=C++]
struct StaticDep {
  int[] reads;
  int[] writes;
}

function findLongestPath(int[] trace, map<int, StaticDep> deps) -> int {
  int[] curDeps = [0,...,0];

  int maxChainLength = 0;

  for (int instr : trace) {
    int chainLength = 1;

    /* Examine read dependencies */
    for (int reg : deps[instr].reads) {
      chainLength = max(chainLength, curDeps[reg].snd + 1);
    }

    maxChainLength = max(maxChainLength, chainLength);

    /* Propagate write dependencies */
    for (int reg : deps[instr].writes) {
      curDeps[reg].fst = true;
      curDeps[reg].snd = chainLength;
    }
  }

  return maxChainLength;
}
\end{lstlisting}

\subsection{Verifying the baseline}

%TODO: not sure i want to write like this.
We can verify a result from the algorithm by inspecting a small example.

The \verb|cksum| program trace (Section \ref{prep:datasets}) has a small inner loop of 
7 instructions, which is a basic block that iterates 65536 times each time it is invoked.

\begin{figure}[H]
  \centering
    \begin{minipage}{0.6\textwidth}
      \begin{lstlisting}[frame=none,numbers=none,escapeinside={(*}{*)}, xleftmargin=4em]
        (*\offset{...}*)
        (*\offset{1c68}*) (*\color{gray}\textit{eor \marker{x19_1}x19, x0, x19, lsl \#8}*)
        (*\offset{1c6c}*) (*\color{gray}\textit{cmp x2, x3}*)
        (*\offset{1c70}*) (*\color{gray}\textit{b.ne 1c58}*)
        (*\offset{1c58}*) ldrb (*\marker{w1_1}*)w1, [w2], #1
        (*\offset{1c5c}*) eor (*\marker{x1_1}*)x1, x1, (*\marker{x19_2}*)x19, lsr #24
        (*\offset{1c60}*) and (*\marker{x1_3}*)x1, (*\marker{x1_4}*)x1, #0xff
        (*\offset{1c64}*) ldr (*\marker{x0_1}*)x0, [x23, (*\marker{x1_5}*)x1, lsl #3]
        (*\offset{1c68}*) eor (*\marker{x19_3}*)x19, (*\marker{x0_2}*)x0, x19, lsl #8
        (*\offset{1c6c}*) cmp x2, x3
        (*\offset{1c70}*) b.ne 1c58
        (*\offset{1c58}*) (*\color{gray}\textit{ldrb w1, [w2], \#1}*)
        (*\offset{1c60}*) (*\color{gray}\textit{eor x1 , x1 , \marker{x19_4}x19 , lsr \#24}*)
        (*\offset{...}*)
      \end{lstlisting}
    \end{minipage}
    \begin{tikzpicture}[remember picture, overlay]
        % 1. x19 (eor) -> x19 (eor)
        \draw[deparrow] ($(x19_1)+(0.2,0)$) to ($(x19_2)+(0.05,0.1)$);

        % 2. x1 (eor) -> x1 (and)
        \draw[deparrow] ($(x1_1)+(0.2,0)$) to ($(x1_4)+(0.05,0.1)$);
        
        % 3. x1 (and) -> x1 (ldr)
        \draw[deparrow] ($(x1_3)+(0.35,0.05)$) to ($(x1_5)+(0.05,0.1)$);

        % 4. x0 (ldr) -> x0 (eor)
        \draw[deparrow] ($(x0_1)+(0.35,0.05)$) to ($(x0_2)+(0.05,0.1)$);

        % 5. x19 (eor) -> x19 (eor)
        \draw[gdeparrow] ($(x19_3)+(0.4,0)$) to ($(x19_4)+(0.05,0.1)$);
    \end{tikzpicture}
  \caption{Partially unrolled cksum loop body in Arm Assembly, annotated with critical path dependencies}
  \label{fig:cksum-deps}
\end{figure}


Figure \ref{fig:cksum-deps}
shows the Arm assembly code for cksum's inner loop, including instructions from previous and successive loop iterations.
Data dependencies from written registers to read registers are drawn on the figure in red, showing the critical path 
for one iteration of the loop. Note that the critical path repeats and extends across loop iterations.

Given that there are 7 instructions in the loop body, and the length of the critical path over one body is 4, 
we should expect that the ILP limit is $7/4=1.75$. Executing the sequential algorithm over one invocation of the inner 
loop (65536 iterations) processes a total of 458752 instructions, and finds a critical path length of 262145. This 
path length includes the outgoing edge from the last execution of instruction \verb|1c68|, so 1 is subtracted.
Then, the ILP limit is $458752/262144=1.75$, as expected.

\subsection{Parallelisation?}

%TODO: Rewrite - this is crappy explanation.
Since only the longest chain is stored at each iteration and all other chains discarded, the \verb|curDeps| array 
holding the longest chain length ending at each register is not a valid memoisable result for a small instruction window 
repeating many times in the algorithm. All the longest dependency chains starting at some register and
ending at another have to be encoded in the result, which pushes us towards the matrix representation.
Then, propagation of dependencies for all the chains is simply the multiplication of the matrices.

\section{Matrix Multiplications}

This section details several approaches to representing and multiplying matrices together on the CPU.

\subsection{A naive matrix implementation}

Matrices are typically represented by a 2-dimensional array of elements. Row-major order and column-major order are methods 
for storing multidimensional arrays in linear memory (arrays of a single dimension).

\begin{figure}[H]
  \centering
  \input{diagrams/row-col-major.tex}
\end{figure}

This implementation chooses row-major as the data representation for our matrices. The index of the element $a_{i,j}$ of
an $n\times m$ matrix $A$ in the row-major array representing the matrix is $im + j$.

We can implement a basic matrix multiplication algorithm based on the formula from Definition \ref{def:mat-mul}.

\begin{lstlisting}[mathescape=true]
function multiply(Matrix<n,m> X, Matrix<m,p> Y) {
  Matrix<n,p> V;

  for (i : [0,...,n-1]) {
    for (j : [0,...,p-1]) {
      int sum = $-\infty$;
      for (k : [0,...,m-1]) {
        sum = max(sum, X[i,k] + Y[k,j]);
      }
      V[i,j] = sum;
    }
  }

  return V;
}
\end{lstlisting}

%Modern hardware prefetcher can more easily recognise consecutive access rather than a stride access.

Modern CPU caching exploits spatial locality of reference, where if a piece of data is accessed by the CPU, other pieces
of data in close proximity in memory will be in the same cache line or prefetched into the cache, therefore reducing the
chance of an expensive cache miss on subsequent cycles.
In the chosen algorithm, matrices $X$ and $Y$ are both stored in row-major order, yet inside the inner loop, $X$ is
being traversed row-wise, and $Y$ is being traversed column-wise. The elements of Y being accessed on consecutive
iterations are not in close proximity to each other in memory, and so cache misses dominate the running time, rather
than the actual calculations.

This issue can be avoided by storing matrices in both row-major and column-major order. Then, depending on whether the
matrix is the right or left operand in a multiplication, we can select the most cache-friendly option to traverse.
This however, doubles our memory usage, and so we instead try to change the order in which we access elements of the matrices.

\subsection{Traversal order and cache performance} \label{impl:traversal-order}

The three loops in the previous matrix multiplication can be arbitrarily swapped with each other without an effect 
on correctness or asymptotic running time. We can swap our $k$ and $j$ loops so that the inner loop traverses $Y$ in 
row-major order, resulting in the following algorithm:

\begin{lstlisting}[mathescape=true, language=C++]
function multiply(Matrix<n,m> X, Matrix<m,p> Y) {
  Matrix<n,p> V;

  for (i : [0,...,n-1]) {
    for (k : [0,...,m-1]) {
      int xVal = X[i,k];
      if (xVal == $-\infty$) continue;

      for (j : [0,...,p-1]) {
        V[i,j] = max(V[i,j], xVal + Y[k,j]);
      }
    }
  }

  return V;
}
\end{lstlisting}

Apart from the improved cache performance, there are three other optimisations.

\begin{description}
  \item[Value hoisting] Since the indices $i$ and $k$ do not change in the inner loop of the algorithm, we can hoist the 
    retrieval of $X_{i,k}$ outside of the loop.
  \item[Sparsity] This optimisation concerns line 7 of the algorithm. To make sense of it, we can consider an extension
    to the previous matrix multiplication definition:
    $$
    v_{i \cdot} = \displaystyle\sum_{k=1}^{n} \! {}_{\oplus} x_{ik} \odot y_{k\cdot}
    $$
    This says that the $i$th row of $V$ is a linear combination of the $k$ rows of $Y$. More importantly, note that 
    $$
    -\infty \odot
    \begin{bmatrix}
      y_{k 1} \\
      \vdots \\
      y_{k p}
    \end{bmatrix}
    =
    \begin{bmatrix}
      -\infty \\ 
      \vdots \\ 
      -\infty
    \end{bmatrix}
    $$
    and since $-\infty$ is the additive identity, this row does not contribute to the final sum, and so the dot product 
    can be skipped. So, more precisely, the $i$th row of $V$ is a linear combination of the $k$ rows of $Y$ for which 
    $x_{ik}$ is not $-\infty$ \cite{gustavson-sparse}. The matrices in this project tend to be fairly sparse and so this 
    results in a significant performance improvement.
  \item[Vectorisation] Finally, since $Y$ is traversed contiguously in the inner loop, this makes it easily open to 
    optimisations using vector instructions. Vector instructions, also known as SIMD (Single-Instruction, Multiple-Data) \cite{flynntaxonomy},
    perform the same operation on several operands, exploiting data-level parallelism. The max and addition operations can 
    then be performed on the adjacent data from several iterations of the inner loop using vectorised variants, increasing 
    the number of elements processed per iteration.
\end{description}

\subsection{Matrix tiling}

%TODO: Do i need a more in-depth explanation?

One other common method of improving the cache performance of matrix multiplication algorithms is \textit{tiling}.
This involves implicitly dividing the matrix into square tiles of smaller sizes, and instead of traversing an entire 
row and column of the operands in one sweep to compute a cell of the output matrix, the algorithm works only on the values 
within one tile, before moving onto the next. This improves the spatial locality of memory accesses, as elements at the 
beginning of a row are not evicted by the time we get to the end of the row, which is a problem for large matrices.

However, since we are only tracking register dependencies in this project, our matrices are only approximately $64 \times 64$
in size. Assuming each cell holds a 32-bit integer, an entire matrix is stored in $64\cdot 64 \cdot 4 =16384$ bytes, 
which can be easily held in the 32 or 64KiB L1 cache of a modern processor core.

In this case, tiling the matrix will actually result in unnecessary overhead from a more complex control flow.

\textit{\textbf{Note for supervisor} - i am not sure whether this section contradicts my previous comments on cache performance 
and changing the traversal order. Would it be more accurate to say that while the entire matrix can be stored in 
a fast cache, the contiguous traversal order is easier for prefetchers to recognise and load data for? Contiguous 
accesses would also often grab from the same cache line, could this be another benefit? And then, matrix tiling is not 
so useful as it is more focused on keeping the working set in cache rather than exhibiting a friendly access order.}

\subsection{Sparse Matrices}

In the previous matrix multiplication algorithm, we presented an optimisation that exploited the sparsity of matrices.
This provided a significant speedup to multiplications, and so implies we may be able to extract further benefits from 
the sparsity.

First, we can analyse the sparsity of all the matrices we encounter over the course of calculating the ILP.

%TODO: Diagram + average nni
\textit{insert diagram here}

This shows that a significant proportion of the matrices we encounter are composed of negative infinity values.
It turns out we can get away without having to store these values, therefore saving on memory space.

\subsubsection{CSR Matrix Format}

There exist many formats for sparse matrix representation. We selected CSR (Compressed Sparse Row) format, as it is also 
used in the chosen multiplication algorithm presented by Gustavson \cite{gustavson-sparse}.

Unlike most sparse matrix representations, which only store nonzero values, we will be storing only
non-negative-infinity values, as we are working in the tropical semiring. We quantify the `sparsity' of a matrix using 
its $nni$ number, the number of values in the matrix that are not $-\infty$.

%TODO: This might be too verbatim from the source.
The CSR representation for a $m\times n$ matrix $A$ is given by three one-dimensional arrays. Two arrays list column 
indices ($JA$) and numerical values ($A$) of the \textit{non-negative-infinity} matrix entries. The arrangement of 
these elements is row-wise. The third array is a set of row address pointers ($IA$) where the $i$th element of $IA$ is 
the address in both $JA$ and $A$ of the first \textit{non-negative-infinity} element of the $i$th row of $A$.

For example, the matrix:

$$
\begin{pmatrix}
  5 & -\infty & -\infty & -\infty \\ 
  -\infty & 8 & -\infty & -\infty \\ 
  -\infty & 1 & 3 & -\infty \\ 
  -\infty & 6 & -\infty & -\infty \\
\end{pmatrix}
$$

Is stored as:

\begin{lstlisting}[frame=none,numbers=none]
IA = [0, 1, 2,    4, 5]
JA = [0, 1, 1, 2, 1]
A =  [5, 8, 1, 3, 6]
\end{lstlisting}

To extract a row $i$, we define:

\begin{lstlisting}[frame=none,numbers=none]
rowStart = IA[i]
rowEnd   = IA[i + 1]
\end{lstlisting}

Then we take slices from $JA$ and $A$ starting at \verb|rowStart| and ending just before \verb|rowEnd|.

In this case the CSR representation contains 15 entries, compared to 16 in the original dense representation. 
The CSR format saves on memory only when $nni < (m (n - 1) - 1)/2$.

\subsubsection{Sparse Matrix Multiplication}

Gustavson \cite{gustavson-sparse} proposes an algorithm for multiplying two matrices $A$ and $B$ in CSR format.
Again, as $B$ is stored in row-wise it is more efficient to traverse it row-wise, and so the algorithm depends on the 
linear combination formulation presented in Section \ref{impl:traversal-order}.
To find the $i$th row of $C$, for each non-negative-infinity value $A_{ik}$, scale the row $B_{k\cdot}$ by $A_{ik}$,
and add it to the current row of $C$.

The challenge for the sparse representation is adding these sparse rows together efficiently. To do this, Gustavson 
introduces a technique using a dense accumulator and a `switch' array. The dense accumulator \verb|x| is an array used 
to sum up values for the current row. The switch array \verb|xb| is an array used to track which columns have been 
touched in the current row calculation.

\begin{lstlisting}[mathescape=true,language=C++]
struct SparseMatrix<m,n> {
  int rows = m;
  int cols = n;
  int[] i; // (IA)
  int[] j; // (JA)
  int[] v; // (A)
}

function multiply(SparseMatrix<m,n> A, SparseMatrix<n,p> B) {
  int[p] x  = [$-\infty$,...,$-\infty$];
  int[p] xb = [-1,...,-1];
  Queue<int>  iCols;

  SparseMatrix<m,p> C;

  C.i[0] = 0;

  for (i : [0,...,m-1]) {
    for (jp : [A.i[i],...,A.i[i+1]-1]) {
      int j = A.j[jp];
      int a = A.v[jp];

      for (kp : [B.i[j],...,B.i[j+1]-1]) {
        int k = B.j[kp];
        int b = B.v[kp];

        if (xb[k] != i) {
          xb[k] = i;
          x[k] = a + b;
          iCols.push(k);
        } else {
          x[k] = max(x[k], a + b);
        }
      }
    }

    while (!iCols.empty) {
      int col = iCols.pop();
      // Assume dynamically allocated arrays
      C.j.push(col);
      C.v.push(x[col]);
    }

    C.i[i+1] = C.j.size;
  }

  return C;
}
\end{lstlisting}

The accumulation is then done as follows:

\begin{itemize}
  \item If \verb|xb[k] == i|, we have already seen this column for the current row. Simply add the value to the 
    accumulator \verb|x|.
  \item Otherwise, this is the first time seeing this column for the current row. Initialize \verb|x|, update \verb|xb|
    to $i$ and store the column index in a group of active columns.
  \item After the accumulation is complete, the algorithm copies the active columns and their associated values into the 
    new sparse matrix.
\end{itemize}

The algorithm requires dynamic allocation for $C$'s structures, as the $nni$ of $C$ is not known until the
multiplcation is carried out.

\section{The matrix approach}

%Show how matrices are stored - ref counting

\section{Expression reduction}

This section describes several data-driven optimisations for reducing the work required to reduce the matrix expression,
by exploiting patterns.

\subsection{Fast exponentiation}

Fast exponentiation is a method that allows fast computation of large positive integer powers of a square matrix.
It is based on the recursive equation:

$$
A^{n} = \begin{cases}
  A &\text{ if } n=1 \\
  A\odot(A^{(n-1)/2})^{2} &\text{ if } n>1 \text{ is odd}\\
  (A^{n/2})^{2} &\text{ if } n \text{ is even}
\end{cases}
$$

This reduces the number of matrix multiplications required to evaluate $A^n$ from $O(n)$ to $O(\log n)$.

\subsection{Pair encoding}

The next optimisation aims to maximise the effectiveness of a single matrix multiplication by exploiting its frequency 
in the broader expression. The intuition is that by memoising the result of the most frequent adjacent matrix pair, 
we can replace all instances of that pair throughout the expression, thereby doing the least amount of work to reduce
the expression by the largest amount.

This results in a multi-pass algorithm. During each iteration, the algorithm scans the expression to find the most 
frequent adjacent pair of matrices. It then computes the result of their multiplication, and replaces all 
instances of the pair with the result matrix. This process is repeated until the maximum frequency of a pair reaches a
specified cutoff threshold.

This approach directly mirrors dictionary-based compression algorithms, specifically Byte Pair Encoding (BPE) \cite{GageBPE}.
BPE iteratively identifies and substitutes the most frequently occurring adjacent symbols in a sequence to minimise 
data size. This reflects the core principle of entropy coding, which achieves an optimal code by assigning the smallest 
representational cost (shortest codes) to the most frequent occurrences.

As a consequence, this algorithm is able to effectively exploit the patterns outlined in Section \ref{prep:datasets}.
For example, returning to cksum's hot loop of 7 instructions:

$$
(A B C D E F G)^{65536}
$$

All adjacent pairs of matrices in the hot loop will have the same frequency of 65536. When that is the maximum frequency, 
the algorithm will compute $A\odot B=X$, and reduce the loop to

$$
(X C D E F G)^{65536}
$$

After several more passes, the loop body will be reduced to a single matrix, raised to the power of 65536, which can then 
be rapidly evaluated using the fast exponentiation approach.

\subsection{Multiple pair encoding}

While the described pair encoding scheme ensures the most frequent adjacent pair is selected at each iteration,
computing the frequencies can be expensive. As we evaluate the larger data patterns in the earlier stages of the
reduction, the top frequency falls dramatically, and simply evaluating the most frequent adjacent pair does not offset
the cost of performing the frequency analysis.

We can mitigate the cost by choosing to evaluate many adjacent pairs at an iteration, increasing the amount of work done,
and so reducing the number of iterations required for this stage. The approach we take is to find the top $N$ most 
frequent pairs, and set them as \textit{candidates} for replacement. Not all chosen pairs will end up being evaluated, 
as some may disappear during replacement as they are adjacent to other evaluated pairs.  %TODO: unclear wording
However, this approach is less accurate and can impact the ability to exploit data patterns.

We again inspect cksum's hot loop. We can unroll the loop body once and lay it out like so
$$
(A B C D E F G A B C D E F G)^{32768}
$$

Choosing to replace 8 pairs ($AB\to H, CD \to I, \dots$), one iteration reduces the expression to

$$
(H I J K L M N)^{32768}
$$

We can repeat this process, unrolling the new loop body again, performing 8 multiplications,
and halving the size each time. This will require $8 \cdot \log_2(65536) + 6 = 30$ multiplications \textit{and}
$\log_2(65536) = 16$ frequency analyses.
By contrast, replacing one pair per iteration requires $6+\log_2(65536) = 22$ multiplications, and $6$ frequency analyses.
This is because the reduction happens within the boundaries of the loop body, and reduces it to one matrix, 
allowing our fast exponentiation algorithm to work.

\subsection{Block preprocessing}

Initially, the expression held one matrix per instruction in the instruction trace, even though the original program
trace was provided as a series of basic blocks. We can instead use the information about the program's basic blocks to
build the expression with individual matrices representing the dependency information for an entire basic block, before 
passing the expression to the encoding stage.

This is implemented as a preprocessing stage which fetches a basic block, multiplies the matrices representing all the
instructions in the block, adds the result matrix to the expression, and caches it for if the same block is encountered 
again.

This directly avoids reduction occurring across basic block boundaries, and so brings the performance of the multiple 
pair encoder in line with the single pair encoder. An additional benefit is the reduction in size of the expression 
passed to the encoder, which not only improves frequency analysis performance, but reduces memory usage.

However, for loop structures with a basic block period $>1$ (see \verb|factor| in Section \ref{prep:datasets}),
we can still get reduction happening across the boundaries of the loop body's period.

%TODO: The above is unclear/bad wording

\subsection{Variable pair encoding}

This variant of the pair encoding scheme attempts to resolve two issues with the multiple pair encoder, while avoiding 
the poor performance of the single pair encoder in later iterations of the encoding phase.

\begin{description}
  \item[Poor accuracy with loop structures] As established previously, the multiple pair encoder will not respect 
    loop boundaries, and evaluating too many adjacent pairs will combine several iterations of a loop together, instead 
    of strictly reducing within the loop body.
  \item [Unclear parameter effect] The `top $N$ pairs' parameter of the multiple pair encoder tends to be opaque, and 
    its optimal value varies significantly between datasets, as it depends on the internal patterns in the data, not 
    the length of the trace.
\end{description}

The variable pair encoder still replaces multiple pairs per iteration, but chooses the count dynamically. In the 
frequency analysis stage, the encoder builds a max heap of all unique pairs in the expression, sorted by frequency 
descending. The encoder then pops pairs off the heap, summing the frequencies, until the total frequency of the candidates 
exceeds a proportional threshold, chosen as a fixed percentage of the expression size.

We can see how this addresses particular pain points.

\begin{itemize}
  \item In earlier iterations, repeated patterns such as loops dominate the expression, and so the frequencies of 
    particular adjacent pairs are very high. The encoder will choose a small number of candidate pairs as their total
    frequency will exceed the threshold quickly. This avoids replacing enough pairs to reduce across loop boundaries.
  \item In later iterations, the frequencies of adjacent pairs plummets, and so the encoder will choose many more 
    candidate pairs to exceed the threshold. This ensures enough work is done per iteration to offset the cost of the 
    frequency analysis.
  \item Finally, the optimal fixed percentage parameter varies much less across different datasets, as evidenced in %TODO: EVALUATION
\end{itemize}

\subsection{Finding loops without frequency analysis}

\section{Thread-level parallelism}

\section{Accelerator Compute}

